#ifndef PLAYER_H
#define PLAYER_H

// Places the main bet for a round of Blackjack.
double placeBet(double *playerMoney);

// Optional: place a side bet, for example, on getting a straight flush.
double placeSideBet(double *playerMoney);

#endif
